# 원격 동기화 (Cloudflare Worker 배포 URL + API_SECRET) — 둘 다 있으면 localStorage 대신 D1 사용
# Vercel 프로젝트 Environment Variables에도 동일하게 설정 (Production / Preview)
# VITE_API_URL=https://369stage-api.<subdomain>.workers.dev
# VITE_API_SECRET=배포 시 wrangler secret put 과 동일한 값
