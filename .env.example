# 원격 API (Cloudflare Worker) — 둘 다 있으면 브라우저가 Worker와 동기화합니다.
# 이 파일을 복사: cp .env.example .env.local  후 VITE_API_SECRET 만 채우세요.

VITE_API_URL=https://369stage.369stage.workers.dev
# Worker 배포 시 wrangler secret put API_SECRET 으로 등록한 값과 동일
VITE_API_SECRET=
